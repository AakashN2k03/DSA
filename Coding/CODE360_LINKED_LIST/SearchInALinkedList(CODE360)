CODE360:  Search in a Linked List (SLL)
 
int searchInLinkedList(Node<int> *head, int k) {
    Node<int>* temp;
    temp=head;
    while(temp)
    {
        if(temp->data==k) return 1;
        temp=temp->next;
    }
    return 0;
}

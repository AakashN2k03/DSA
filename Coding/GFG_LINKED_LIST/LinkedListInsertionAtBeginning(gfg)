GFG: Linked List Insertion At Beginning (SLL)

 Node *insertAtBegining(Node *head, int x) {
        
        if(head==NULL)
        return new Node(x);
        
        Node *temp=new Node(x);
        temp->next=head;
        return temp;
    }
};
